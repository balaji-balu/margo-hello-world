// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/balaji-balu/margo-hello-world/ent/deploymentstatus"
	"github.com/google/uuid"
)

// DeploymentStatus is the model entity for the DeploymentStatus schema.
type DeploymentStatus struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// State holds the value of the "state" field.
	State string `json:"state,omitempty"`
	// ErrorCode holds the value of the "error_code" field.
	ErrorCode string `json:"error_code,omitempty"`
	// ErrorMessage holds the value of the "error_message" field.
	ErrorMessage string `json:"error_message,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DeploymentStatusQuery when eager-loading is set.
	Edges        DeploymentStatusEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DeploymentStatusEdges holds the relations/edges for other nodes in the graph.
type DeploymentStatusEdges struct {
	// Components holds the value of the components edge.
	Components []*DeploymentComponentStatus `json:"components,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ComponentsOrErr returns the Components value or an error if the edge
// was not loaded in eager-loading.
func (e DeploymentStatusEdges) ComponentsOrErr() ([]*DeploymentComponentStatus, error) {
	if e.loadedTypes[0] {
		return e.Components, nil
	}
	return nil, &NotLoadedError{edge: "components"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DeploymentStatus) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case deploymentstatus.FieldState, deploymentstatus.FieldErrorCode, deploymentstatus.FieldErrorMessage:
			values[i] = new(sql.NullString)
		case deploymentstatus.FieldCreatedAt, deploymentstatus.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case deploymentstatus.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DeploymentStatus fields.
func (_m *DeploymentStatus) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case deploymentstatus.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case deploymentstatus.FieldState:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field state", values[i])
			} else if value.Valid {
				_m.State = value.String
			}
		case deploymentstatus.FieldErrorCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field error_code", values[i])
			} else if value.Valid {
				_m.ErrorCode = value.String
			}
		case deploymentstatus.FieldErrorMessage:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field error_message", values[i])
			} else if value.Valid {
				_m.ErrorMessage = value.String
			}
		case deploymentstatus.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case deploymentstatus.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DeploymentStatus.
// This includes values selected through modifiers, order, etc.
func (_m *DeploymentStatus) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryComponents queries the "components" edge of the DeploymentStatus entity.
func (_m *DeploymentStatus) QueryComponents() *DeploymentComponentStatusQuery {
	return NewDeploymentStatusClient(_m.config).QueryComponents(_m)
}

// Update returns a builder for updating this DeploymentStatus.
// Note that you need to call DeploymentStatus.Unwrap() before calling this method if this DeploymentStatus
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *DeploymentStatus) Update() *DeploymentStatusUpdateOne {
	return NewDeploymentStatusClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the DeploymentStatus entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *DeploymentStatus) Unwrap() *DeploymentStatus {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: DeploymentStatus is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *DeploymentStatus) String() string {
	var builder strings.Builder
	builder.WriteString("DeploymentStatus(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("state=")
	builder.WriteString(_m.State)
	builder.WriteString(", ")
	builder.WriteString("error_code=")
	builder.WriteString(_m.ErrorCode)
	builder.WriteString(", ")
	builder.WriteString("error_message=")
	builder.WriteString(_m.ErrorMessage)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// DeploymentStatusSlice is a parsable slice of DeploymentStatus.
type DeploymentStatusSlice []*DeploymentStatus
